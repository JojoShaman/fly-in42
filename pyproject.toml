[project]
name = "fly-in"
version = "0.1.0"
description = "working on it"
requires-python = ">=3.10"
dependencies = [
    "pydantic >= 2.13.4",
    "pygame >= 2.6.1"
]

[tool.poetry.dependencies]
python = ">=3.10"

[tool.poetry]
package-mode = false